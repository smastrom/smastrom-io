---
import IconArrow from './icons/IconArrow.astro'

import { AUTHOR_EMAIL } from '@/lib/constants'
---

<a href={'mailto:' + AUTHOR_EMAIL} target="_blank"> Get in Touch <IconArrow /> </a>

<style>
   a {
      display: inline-flex;
      align-items: center;
      gap: var(--s-05);
      text-decoration: none;
      color: var(--color-fg);
      font-weight: 500;
      white-space: nowrap;
      font-size: var(--f-2);
      transition: opacity var(--ease-fast);
      margin-bottom: var(--s-4);

      @media (--tablet-to) {
         margin-bottom: var(--s-1);
      }

      & :global(svg) {
         transition: transform var(--ease-fast);
         color: var(--color-accent);
      }

      @media (hover: hover) {
         &:hover {
            opacity: var(--o-3);
            color: var(--color-accent);

            & :global(svg) {
               transform: translateX(var(--s-02));
            }
         }
      }
   }
</style>
