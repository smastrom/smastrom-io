---
import Head from '@/components/Head.astro'
import Header from '@/components/Header.astro'
import Footer from '@/components/Footer.astro'
import Background from '@/components/Background.astro'
import JsonLd from '@/components/JsonLd.astro'

import { useDato } from '@/lib/dato-cms'
import { getSharedJsonLd, getPageJsonLd } from '@/lib/json-ld'

import type { CmsLayout } from '@/lib/types'

import '@/styles/reset.css'
import '@/styles/colors.css'
import '@/styles/layout.css'
import '@/styles/typography.css'
import '@/styles/hamburger.css'
import '@/styles/classes.css'
import '@/styles/smastrom-variables.css'

const { data, error } = await useDato<CmsLayout>(/* GraphQL */ `
   {
      _site {
         globalSeo {
            fallbackSeo {
               title
               description
            }
         }
      }
      layout {
         footerText(markdown: true)
      }
   }
`)

if (error) throw new Error(error)

const { fallbackSeo } = data._site.globalSeo

const { title = fallbackSeo.title, description = fallbackSeo.description } = Astro.props as {
   title: string
   description: string
}

const sharedJsonLd = getSharedJsonLd()
const pageJsonLd = getPageJsonLd(Astro.url.pathname, { title, description })
---

<!doctype html>
<html lang="en" dir="ltr" data-theme="auto" class="g-transitions">
   <Head {title} {description}>
      <JsonLd slot="head-end" data={sharedJsonLd} />
      {pageJsonLd && <JsonLd slot="head-end" data={pageJsonLd} />}
      <slot name="head-end" />
   </Head>

   <body>
      <Background />
      <a href="#main" class="g-sr-only">Skip to main content</a>

      <Header />

      <div id="content">
         <main id="main">
            <slot />
         </main>

         <Footer content={data.layout.footerText} />
      </div>
   </body>
</html>
